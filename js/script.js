// CSS 
// done = line trough
// check completed = green mark + circle
// check uncompleted = red cross + circle

const { createApp } = Vue;

createApp ({
    data(){
        return {
            
        }
    },
    methods: {

    }
}).mount("#app")